# Правки инвестор-презентации

## 1. Глобальные правки (все слайды)

**Светлая тема**
- Фон: `#F7F4EC` (тёплый кремовый), поверхности карточек `#FFFFFF`, границы `#D5A52A/30`.
- Текст: основной `#1B1D22`, приглушённый `#1B1D22/70`, акцент остаётся золотой `#D5A52A`.
- Правки в `SlideLayout.tsx` (фон, цвет по умолчанию) и в `InvestorDeck.tsx` (обёртка).
- Пройтись по всем слайдам и заменить хардкод `#F5F1E8`, `#25272D`, `#1B1D22`, `bg-[#1B1D22]` на светлую палитру.

**Минимальный размер шрифта 11px**
- Аудит всех `text-[Npx]` в слайдах: `text-[11px]` — минимум, поднять `10px→11px`, `12px→12px` оставить, `13px` оставить. Максимум 60px — уже соблюдается.

**Никаких английских терминов**
Замены:
- `CAC` → «Стоимость привлечения»
- `LTV` → «Пожизненная ценность клиента»
- `ARPU` → «Средний доход с клиента»
- `ARR` → «Годовая выручка»
- `Payback` → «Окупаемость»
- `Gross margin` → «Валовая маржа»
- `Y1/Y2/Y3` → «1-й год / 2-й год / 3-й год»
- `seat` → «сотрудник», `per-seat` → «за сотрудника»
- `PoC` → «пилот»
- `ICP` → «целевой профиль клиента»
- `SEO` → «поисковый трафик», `LMS` → «онлайн-обучение», `ATS` → «система найма», `KPI` → «показатели»
- `OKR` → «цели и результаты», `IDP` → «план развития»
- `HR / HRD / CHRO / CIO / CFO` — оставляем (устоявшиеся в РФ).
- `AI` → «ИИ», `Fullstack-разработчик` → «Разработчик», `Sales Lead` → «Руководитель продаж», `SDR` → «Специалист по продажам», `Tech Lead` → «Технический лидер», `Product Owner` → «Владелец продукта».
- `Q1..Q4` → «1-й квартал…» и т.д.
- Заголовок «Go-to-market» → «Выход на рынок».

**Все тексты редактируемые**
- Проверить, что каждый строковый узел в слайдах 1–6 обёрнут в `<Editable id=… defaultValue=… />` (включая: заголовки каналов на слайде 6, названия этапов, ссылки-источники на слайде 2, метки метрик юнит-экономики, подписи в дереве на слайде 3, названия квартальных этапов и т.д.). Сейчас во многих местах контент захардкожен.
- Ссылки-источники: сделать редактируемыми и текст, и `href` (добавить поле `<Editable id=".url" />` рядом или новый компонент `EditableLink`).

**Везде «100+ сотрудников»** (заменить `200+`, `250+`, `500+` на `100+` в слайдах 2, 5, 6 и заголовках карточек).

## 2. Слайд 2 «Рынок»

- Удалить блок графика «Компании 250+ сотрудников, тыс.» (`companiesGrowth`) целиком.
- Освободившееся место отдать двум оставшимся блокам (график HR-tech + «Ежедневные боли HRD»): раскладка `col-span-6 / col-span-6`.
- Нижние блоки (три карточки со статистикой) увеличить на ~40%: сейчас `px-8 py-6`, число `60px` — сделать `px-11 py-9`, число `84px` (в пределах максимума 60→ уточняем: контейнер +40%, шрифт числа +40% но не выше 60px, значит остаётся 60px; вырастают отступы, размер юнита с 24→32px, подпись с 16→20px).
- Обновить `k1..k3` карточки: «Юр. лиц в РФ со штатом **100+**».

## 3. Слайд 3 «Дерево платформы» — полная переделка

Новая структура (4 ветки вместо 9 модулей):

```
Основа (ствол): «Портал компании»
  под-элементы (в блоке-основании): Мессенджеры · Новости компании · Таск-трекер

Ветка 1 · Онлайн-университет
  - Онбординг новых сотрудников
  - Профильное обучение
  - Обучение новым продуктам компании
  - Обязательные курсы и аттестации

Ветка 2 · Кадровый резерв
  - Карьерные треки
  - Оценка эффективности (перформанс-ревью)
  - Рейтинг сотрудника (негативные оценки, отставание по задачам)
  - План индивидуального развития

Ветка 3 · Геймификация
  - Награды за достижения
  - Сценарии наград
  - Магазин наград
  - Рейтинги и бейджи

Ветка 4 · HR-аналитика
  - Риски по сотрудникам
  - Текучесть кадров
  - Индекс комфорта и вовлечённости
  - Прогноз выгорания
```

- Переписать `modules[]` в `Slide3ProductTree.tsx`: 4 ветки, у каждой 4 листа-подмодуля.
- Позиционирование: 2 слева / 2 справа, крупные читаемые ноды (шрифт узла 24px, подписи 14px, минимум 11px).
- Основание/ствол: подпись «Портал компании», под ней три подписи через «·».
- Все подписи в SVG вынести из хардкода в `<Editable>` (через `foreignObject` с обёрткой) либо в JSON-конфиг с ключами `s3.b1.name`, `s3.b1.leaf.0` и т.д. — чтобы попадали под требование «все тексты редактируемые».

## 4. Слайд 5 → разделить на два слайда

**5a · «Затраты» (бюджет)**
- Оставить: команда (ФОТ) с калькулятором + добавлением, продвижение, инфра, донат «Итого бюджет».
- Убрать блок юнит-экономики и точки безубыточности отсюда.
- Все английские метки на русском (см. глоссарий).

**5b · «Планируемый доход в структуре»**
- Новый файл `Slide5bRevenue.tsx`.
- Верх: параметры (редактируемые) — «Цена за сотрудника / мес», «Среднее число сотрудников у клиента», «Валовая маржа %».
- Метрики: «Средний доход с клиента / год», «Валовая маржа», «Стоимость привлечения», «Окупаемость», «Пожизненная ценность / стоимость привлечения».
- Таблица роста по годам: 1-й / 2-й / 3-й год — число клиентов (редактируемое) → расчёт годовой выручки.
- Блок «Точка безубыточности» с формулой.
- Всё считается реактивно из тех же полей (`useDeckNumber`) — при правке цены/маржи/клиентов метрики пересчитываются.
- Добавить слайд в массив `slides` в `InvestorDeck.tsx` и в `titles`.

## 5. Слайд 6 → «График получения платежа от компании»

Заменить содержимое `Slide6Sales.tsx`:

Горизонтальный график-таймлайн от контакта до оплаты:

```text
Неделя 0 ─── 2 ─── 4 ─── 6 ─── 8
[Демо]      [Подтверждение +      [Боевой запуск →
             поддержка подключения] оплата]
```

- Три этапа-карточки с диапазонами недель:
  1. «2–4 недели · Работа с демо» — презентация продукта, доступ к песочнице, ответы на вопросы.
  2. «4–6 недели · Подтверждение заинтересованности» — согласование условий, техническое сопровождение подключения, настройка под клиента.
  3. «6–8 недели · Боевой запуск» — миграция данных, обучение админов, **получение оплаты**.
- Визуализация: горизонтальная полоса-градиент с тремя вехами (кружки на 2, 4, 6, 8 нед.), под ней три равные карточки-этапа.
- Ниже — «Итоговый цикл сделки: 6–8 недель до первой оплаты».
- Все тексты и цифры недель — редактируемые.
- Удалить: 5 каналов продаж, дорожную карту Q1–Q4, блок «Почему выигрываем».

## 6. Новый слайд «Что сделано / что предстоит»

Новый файл `Slide7Status.tsx`, добавить в конец колоды.

Две колонки:

**✓ Уже реализовано**
- Портал компании: мессенджер, новости, таск-трекер
- Онбординг и адаптация
- Карьерные треки и оценка компетенций
- HR-аналитика (риски, комфорт, выгорание)
- Геймификация: магазин наград, баллы, бейджи
- Оргструктура и цифровой паспорт
- ИИ-оценка через интервью
- Мультитенантность и 5 ролей
- Мобильная версия (PWA)

**◷ Предстоит сделать**
- Онлайн-университет: полноценный конструктор курсов
- Внешние интеграции: 1С:ЗУП, Битрикс24, календари
- Расширенная аналитика для совета директоров
- On-premise поставка и ИИ на инфраструктуре клиента
- Мобильные нативные приложения iOS/Android
- Маркетплейс сценариев оценки и наград

Каждый пункт — `<Editable>`. Можно добавить прогресс-бар «≈ 70% продукта готово».

## 7. Технические детали

**Файлы к правке:**
- `src/pages/investor/SlideLayout.tsx` — светлая тема.
- `src/pages/investor/InvestorDeck.tsx` — добавить 5b и 7 в `slides[]` и `titles[]`, обновить фон.
- `src/pages/investor/DeckNav.tsx` — цвета под светлую тему.
- `src/pages/investor/slides/Slide1Product.tsx` — светлая тема, русификация, шрифты ≥ 11px.
- `src/pages/investor/slides/Slide2Market.tsx` — удалить один график, увеличить нижние карточки, «100+», editable-ссылки.
- `src/pages/investor/slides/Slide3ProductTree.tsx` — новая структура дерева (4 ветки), editable-подписи.
- `src/pages/investor/slides/Slide4AI.tsx` — светлая тема, русификация («AI»→«ИИ» и т.п.).
- `src/pages/investor/slides/Slide5Economics.tsx` — оставить только «Затраты».
- `src/pages/investor/slides/Slide5bRevenue.tsx` **(новый)** — доход и юнит-экономика.
- `src/pages/investor/slides/Slide6Sales.tsx` — переделать в таймлайн получения оплаты.
- `src/pages/investor/slides/Slide7Status.tsx` **(новый)** — сделано/предстоит.
- `src/pages/investor/deck/Editable.tsx` — светлые цвета outline при редактировании.
- Возможно новый `EditableLink.tsx` для редактируемого `href`.

**Порядок слайдов после правок:** Продукт → Рынок → Архитектура → ИИ → Затраты → Доход → Оплата → Статус (8 слайдов).

## Открытые вопросы

Ни одного критичного — приступаю к реализации после подтверждения.
