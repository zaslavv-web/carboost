## Что означает этот ответ бэкенда

`{created: 0, updated: 1, mailed: 1, skipped: 0, errors: []}` — это **успех**, а не ошибка:

- `created: 0` — новая строка в `employee_invitations` не создана;
- `updated: 1` — по этому email в вашей компании **уже была** запись со статусом `pending`, бэкенд её обновил (новый токен, свежие поля) вместо дубликата;
- `mailed: 1` — письмо ушло в SMTP успешно;
- `errors: []` — ошибок нет.

Логика идемпотентности сейчас в `RpcController::bulkInviteEmployees` (строки 254–302): при повторной отправке на тот же адрес мы не плодим дубликаты, а «переиспользуем» существующее `pending`‑приглашение. Фронт для случая `created === 0 && updated > 0` показывает тост «Приглашение отправлено повторно».

Прежде чем что-то менять, мне нужно понять, что именно вас не устраивает — от этого зависит план. Возможные варианты:

### Вариант A. «Я приглашаю нового человека, а система говорит „повторно“»

Скорее всего, этот email уже есть в `employee_invitations` со статусом `pending` (мог быть создан раньше — например, во время предыдущих отладок или сидов). Тогда нужно:

1. Проверить в БД, есть ли для этой пары `(company_id, email)` строка в `employee_invitations` со статусом `pending`.
2. Если это «мусорные» старые приглашения — очистить их командой `invitations:clear-recent` или SQL-запросом, и пригласить заново.
3. UI-правка (по желанию): показывать явное «Такое приглашение уже существует — обновлено и отправлено заново», а не просто «повторно».

### Вариант B. «Тост всё ещё выглядит тревожно / непонятно»

Тогда фикс только на фронте `src/pages/Invitations.tsx`:

- Для `created=0, updated>=1, mailed=updated` — зелёный тост «Приглашение существовало, обновлено и отправлено заново».
- Для `mailed < actionable` — нейтральный warning с кнопкой «Отправить повторно».
- Для `errors[]` — как сейчас, конкретная ошибка.

### Вариант C. «Хочу всегда создавать новое приглашение, а не обновлять»

Это ломает идемпотентность и приводит к дублям. Не рекомендую, но технически можно: убрать ветку `if ($existing && $existing->status === 'pending')` и всегда делать INSERT, добавив в уникальный индекс `status` или отменяя старые pending перед вставкой (`update ... set status='cancelled'`).

### Вариант D. «На самом деле письмо не дошло, хотя `mailed: 1`»

Тогда проблема не в этом ответе, а в SMTP/доставке. Нужно:

1. Посмотреть последние 60 строк `storage/logs/laravel.log` вокруг отправки.
2. Проверить `MAIL_MAILER` / `MAIL_HOST` в `backend-laravel/.env` (сейчас поддерживаются `smtp`, `log`, `array`, `unisender_go`).
3. Проверить папку «Спам» получателя и SPF/DKIM/DMARC на отправляющем домене.

## Что мне нужно от вас, чтобы двигаться дальше

Ответьте одной строкой, какой из вариантов (A/B/C/D) ваш случай, и — если A или D — приложите:

- email, на который отправляли;
- скрин/копию последней записи из `employee_invitations` по этому email (`select id, email, status, created_at, updated_at from employee_invitations where email = '...' order by created_at desc limit 5;`);
- при D — хвост `laravel.log` за момент отправки.

После вашего ответа я сразу выпущу точечный план правок (миграция/бэк/тост) без лишних телодвижений.  
я хочу чтобы выводился точный диагноз отправки:  
1) пользователь новый = Приглашение отправлено  
2) пользователь уже существовал, но не принял приглашение = Пользователю с данным e-mail приглашение уже отправлялось. Отправить повторно? = кнопки да/нет  
3) пользователь уже существовал, и принял приглашение = Пользователю с данным e-mail уже авторизован в системе  
4) e-mail введен не корректно = Вы ошиблись при написании электронной почты, проверьте правильность написания и повторите попытку