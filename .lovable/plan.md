## План

1. **Проверить фактическое состояние БД после `demo:seed**`
  - Посмотреть, сколько записей сейчас в `companies`, `users/employees` и связанных демо-таблицах.  
  1
  - Проверить, есть ли компания `Demo_Doom` или данные были созданы под другим именем/окружением.  
  есть, но удалились все остальные
2. **Проверить сидер восстановления**
  - Разобрать `SeedDemoCompany.php`: где создаётся company, как работает `--reset`, `--name`, `--headcount`.
  - Найти условия, при которых сидер мог создать пользователей, но не вернуть компанию в список UI.
3. **Проверить API списка компаний**
  - Проверить endpoint, который использует `/superadmin/demo-seed` или список компаний.
  - Убедиться, что фильтры/права/`is_verified`/`company_id` не скрывают созданную компанию.
4. **Исправить причину**
  - Если проблема в сидере — поправить создание/привязку компании и добавить понятный вывод после seed.
  - Если проблема в API/UI — поправить выборку, чтобы восстановленная компания отображалась.
  - Если проблема в окружении — добавить preflight-проверку, чтобы команда явно показывала, к какой БД подключена.
5. **Добавить защитную диагностику**
  - В команду `demo:seed` добавить финальную проверку: компания создана, количество сотрудников совпадает, роли/профили привязаны.
  - При несоответствии команда должна завершаться ошибкой с конкретной причиной, а не “успешно”.
6. **Проверка**
  - Запустить безопасные read-only проверки и PHP syntax check.
  - Дать точную команду для сервера: сначала обновить код, затем повторно запустить seed и проверить результат.