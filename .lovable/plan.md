## Часть 1. Что уже работает

- Страница `/invitations` (`src/pages/Invitations.tsx`) полностью реализована: одиночные строки, импорт CSV/XLSX, отправка через RPC `bulk_invite_employees`, список отправленных приглашений с отменой.
- Бэкенд `RpcController::bulkInviteEmployees` уже принимает роли `hrd`, `company_admin`, `superadmin`, `**manager**` (`RpcController.php:171`), поэтому серверный запрет для руководителя отсутствует.
- В сайдбаре пункт «Приглашения» есть у `company_admin` (`AppSidebar.tsx:111`) и `hrd` (`AppSidebar.tsx:149`). У `manager` его нет — это и есть блокер.

## Часть 2. Что меняем (только фронт)

### 2.1 Добавить пункт «Приглашения» в меню руководителя

`фичу разместить в разделе сотрудники над таблицей сотрудники`  
`функционал назвать "добавить нового сотрудника"`

### схема работы функционала идентична функционалу у суперадмина по добавлению новых пользователей, но сотрудники компании могут добавлять новых пользователей только к себе в компанию  
  
Суперадмин может добавлять сотрудников в любую компанию  
сотрудник, который приходит в компанию по приглашению существующего сотрудника компании (не важно какой позиции) обладает правами своей роли только в рамках своей компании

&nbsp;