APP_NAME="Пик Роста"
APP_ENV=production
APP_KEY=base64:8Zr3kQfP2vN7tYxLhJcD6sWmA1bU0eRgKpQyM5oVnXI=
APP_DEBUG=false
APP_URL=https://growth-peak.pro
APP_TIMEZONE=UTC
APP_LOCALE=ru

LOG_CHANNEL=stack
LOG_LEVEL=info

# --- Database ---
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=careertrack
DB_USERNAME=careertrack
DB_PASSWORD=change-me

# --- Cache / Queue / Session ---
CACHE_STORE=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null
REDIS_CLIENT=predis

# --- Sanctum ---
SANCTUM_STATEFUL_DOMAINS=growth-peak.pro,www.growth-peak.pro,localhost:8080
SESSION_DOMAIN=.growth-peak.pro

# --- Google OAuth (заполнить реальными значениями web-client'а из Google Cloud Console) ---
# Authorized redirect URI в консоли Google: https://growth-peak.pro/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://growth-peak.pro/api/auth/google/callback

# --- Frontend URL ---
FRONTEND_URL=https://growth-peak.pro
APP_FRONTEND_URL=https://growth-peak.pro

# --- Mail / SMTP ---
# Основной источник SMTP — БД (email_settings, редактируется суперадмином через админку).
# При пустой/битой записи — fallback на config/service-infra.php (дефолты Яндекса), пароль ниже:
SMTP_PASSWORD=wrtwpknhswvvsxhk

# Legacy MAIL_* — третий уровень fallback, оставлены для совместимости.
MAIL_MAILER=smtp
MAIL_HOST=smtp.yandex.ru
MAIL_PORT=465
MAIL_USERNAME=growthpeak@yandex.ru
MAIL_PASSWORD=cmfkgfphzbbliykr
MAIL_ENCRYPTION=SSL
MAIL_FROM_ADDRESS=growthpeak@yandex.ru
MAIL_FROM_NAME="Пик Роста"

# --- AI Gateway ---
AI_API_URL=https://api.openai.com/v1/chat/completions
AI_API_KEY=
AI_MODEL=gpt-4o-mini

# --- Filesystem ---
FILESYSTEM_DISK=local
