# Авто Let's Encrypt + reverse-proxy для self-hosted Career Track
{$APP_DOMAIN} {
    encode gzip zstd
    reverse_proxy web:80
}

{$API_DOMAIN} {
    encode gzip zstd
    reverse_proxy {$LARAVEL_UPSTREAM:ct-laravel-nginx:80}
}
